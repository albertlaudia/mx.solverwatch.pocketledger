# media/pitch-deck/

Media assets for the 24-slide pitch deck (`deck/slides/output/pocketledger-strategy-and-architecture.pptx`).

**Contents** (when populated):
- `speaker-photos/` — headshots for the "About the team" slide
- `demo-recordings/` — screen recordings (e.g. `day-0-90-second-prototype.mp4` once Epic 5 ships)
- `supporting-graphics/` — custom illustrations or photography referenced from the deck

The deck source itself is in `deck/slides/`. Drop heavy media here so the .pptx stays small.
